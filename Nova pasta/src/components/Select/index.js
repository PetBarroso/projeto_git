// recebimento de props nomeadas
export default function SelectWrapper(props) {
  //o console nos ajuda a verificar o que estamos recebendo por props
  //   console.log(options[0]);
  return (
    <div>
      Olá, eu sou um select
      <select>
        <option>{}</option>
        <option>{}</option>
        <option>{}</option>
      </select>
    </div>
  );
}
